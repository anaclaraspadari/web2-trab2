<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Telefone</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .form-container {
            max-width: 600px;
            margin: 30px auto;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .form-title {
            margin-bottom: 20px;
            text-align: center;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h2 class="form-title">Editar Telefone</h2>
            <form action="/user/<%= id %>/updatePhone/<%= id2 %>" method="POST">
                <div class="mb-3">
                    <label for="phone" class="form-label">Novo Telefone</label>
                    <input type="phone" class="form-control" id="phone" name="phone" placeholder="Deixe em branco para manter o e-mail atual">
                </div>
                <div class="mb-4">
                    <label class="form-label">Principal:</label>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="principal" id="principalSim" value="1">
                        <label class="form-check-label" for="principalSim">Sim</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="principal" id="principalNao" value="0" checked>
                        <label class="form-check-label" for="principalNao">Não</label>
                    </div>
                </div>
                <div class="d-flex justify-content-between">
                    <a href="/user/<%= id %>" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-primary">Atualizar</button>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>